package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// --- Clean Minimalism Brand Palette ---
val TrustNavy = Color(0xFF005FB0)       // Primary Brand Blue (#005FB0)
val ActionBlue = Color(0xFF3F51B5)      // Secondary Indigo
val AccentSaffron = Color(0xFFD97706)   // Warm Gold Accent

// --- Tonal Surfaces (Slate / Light Gray Palette) ---
val PaperDeskBg = Color(0xFFF7F9FC)     // Background (#F7F9FC)
val PaperWhite = Color(0xFFFFFFFF)      // Cards / Surface Level 1
val PaperBorder = Color(0xFFEEF2F6)     // Light Border (#EEF2F6)
val TextDark = Color(0xFF0F172A)        // Slate 900
val TextGray = Color(0xFF64748B)        // Slate 500

// --- Semantic Alerts ---
val LawGreen = Color(0xFF10B981)        // Emerald / Bailable / Approved
val LawRed = Color(0xFFEF4444)          // Red / Non-Bailable / Warning
val LightRed = Color(0xFFFEF2F2)
val LightGreen = Color(0xFFF0FDF4)
val LightYellow = Color(0xFFFEFBF0)

